name: Deploy TravelAI

on:
  push:
    branches: [main]
  pull_request:
    branches: [main]

jobs:
  test-backend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./backend
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Python
        uses: actions/setup-python@v5
        with:
          python-version: '3.11'
      
      - name: Install dependencies
        run: |
          python -m pip install --upgrade pip
          pip install -r requirements.txt
          pip install pytest pytest-asyncio
      
      - name: Run tests
        run: pytest || echo "No tests yet"

  test-frontend:
    runs-on: ubuntu-latest
    defaults:
      run:
        working-directory: ./frontend
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'
          cache-dependency-path: ./frontend/package-lock.json
      
      - name: Install dependencies
        run: npm ci
      
      - name: Build
        run: npm run build

  deploy-docker:
    needs: [test-backend, test-frontend]
    runs-on: ubuntu-latest
    if: github.ref == 'refs/heads/main'
    
    steps:
      - uses: actions/checkout@v4
      
      - name: Set up Docker Buildx
        uses: docker/setup-buildx-action@v3
      
      - name: Build and push Backend
        uses: docker/build-push-action@v5
        with:
          context: ./backend
          push: false
          tags: travelai-backend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max
      
      - name: Build and push Frontend
        uses: docker/build-push-action@v5
        with:
          context: ./frontend
          push: false
          tags: travelai-frontend:latest
          cache-from: type=gha
          cache-to: type=gha,mode=max

  # Deploy to Railway (uncomment to enable)
  # deploy-railway:
  #   needs: [test-backend, test-frontend]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #     - uses: actions/checkout@v4
  #     
  #     - name: Install Railway CLI
  #       run: npm install -g @railway/cli
  #     
  #     - name: Deploy to Railway
  #       run: railway up --service=backend
  #       env:
  #         RAILWAY_TOKEN: ${{ secrets.RAILWAY_TOKEN }}

  # Deploy to AWS ECS (uncomment to enable)
  # deploy-aws:
  #   needs: [test-backend, test-frontend]
  #   runs-on: ubuntu-latest
  #   if: github.ref == 'refs/heads/main'
  #   
  #   steps:
  #     - uses: actions/checkout@v4
  #     
  #     - name: Configure AWS credentials
  #       uses: aws-actions/configure-aws-credentials@v4
  #       with:
  #         aws-access-key-id: ${{ secrets.AWS_ACCESS_KEY_ID }}
  #         aws-secret-access-key: ${{ secrets.AWS_SECRET_ACCESS_KEY }}
  #         aws-region: us-east-1
  #     
  #     - name: Login to Amazon ECR
  #       id: login-ecr
  #       uses: aws-actions/amazon-ecr-login@v2
  #     
  #     - name: Build and push images
  #       run: |
  #         docker build -t ${{ steps.login-ecr.outputs.registry }}/travelai-backend:latest ./backend
  #         docker build -t ${{ steps.login-ecr.outputs.registry }}/travelai-frontend:latest ./frontend
  #         docker push ${{ steps.login-ecr.outputs.registry }}/travelai-backend:latest
  #         docker push ${{ steps.login-ecr.outputs.registry }}/travelai-frontend:latest
  #     
  #     - name: Deploy to ECS
  #       run: |
  #         aws ecs update-service --cluster travelai --service backend --force-new-deployment
  #         aws ecs update-service --cluster travelai --service frontend --force-new-deployment
